	if [ "${pahole_ver}" -ge "118" ] && [ "${pahole_ver}" -le "121" ]; then
		# pahole 1.18 through 1.21 can't handle zero-sized per-CPU vars
		hacksaw_extra_paholeopt="${hacksaw_extra_paholeopt} --skip_encoding_btf_vars"
	fi
>---<
	if [ "${pahole_ver}" -ge "121" ]; then
		hacksaw_extra_paholeopt="${hacksaw_extra_paholeopt} --btf_gen_floats"
	fi
>---<
    if [ "${pahole_ver}" -ge "122" ]; then
        hacksaw_extra_paholeopt="${hacksaw_extra_paholeopt} -j"
    fi
>---<
    if [ "${pahole_ver}" -ge "124" ]; then
        # see PAHOLE_HAS_LANG_EXCLUDE
        hacksaw_extra_paholeopt="${hacksaw_extra_paholeopt} --lang_exclude=rust --skip_encoding_btf_enum64"
		hacksaw_extra_paholeopt="${hacksaw_extra_paholeopt} --skip_encoding_btf_inconsistent_proto --btf_gen_optimized"
	fi
